const express = require("express"); //to build REST APIs
const cors = require("cors"); //Express middleware to enable CORS

const app = express();

// parse requests of content-type - application/json
app.use(express.json());

// parse requests of content-type - application/x-www-form-urlencoded
app.use(express.urlencoded({ extended: true }));

const db = require("./postService/models");
const Uni = db.uni;

db.mongoose
  .connect(`mongodb+srv://ynwin1:3%21OcT2O0%2F@cluster0.q2qmbnh.mongodb.net/?retryWrites=true&w=majority`, {
    useNewUrlParser: true,
    useUnifiedTopology: true
  })
  .then(() => {
    console.log("Successfully connect to MongoDB.");
  })
  .catch(err => {
    console.error("Connection error", err);
    process.exit();
  });

// simple route
app.get("/", (req, res) => {
  res.json({ message: "Setting up Uni" });
});

require('./postService/routes/uni.route')(app);

// set port, listen for requests
const PORT = process.env.PORT || 8080;
app.listen(PORT, () => {
  console.log(`Server is running on port ${PORT}.`);
});